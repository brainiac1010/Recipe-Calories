import PropTypes from 'prop-types';

const Card = ({card}) => {

    console.log(card)
    return (
        <div>
            
        </div>
    );
};

Card.PropTypes={
    card:PropTypes.object.isRequired
}
export default Card;
